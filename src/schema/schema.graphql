type Query {
  allCvs: [Cv!]
  getCv(id: Int!): Cv
  getCvSkills(id: Int!): [Skill!]
  getCvOwner(id: Int!): User
}

enum Role {
  ADMIN
  USER
}

type Cv {
  id: Int!
  name: String!
  age: Int!
  job: String!
  ownerId: Int!
}

type User {
  id: Int!
  name: String!
  email: String!
  role: Role!
}

type Skill {
  id: Int!
  designation: String!
}
